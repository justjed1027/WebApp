// No JS rendering needed; posts are rendered by PHP
document.addEventListener('DOMContentLoaded', function () {
    // text-only inline form submits to post.php — no client-side upload/type logic needed
});
